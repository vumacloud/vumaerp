# ===========================================
# VumaERP Instance Routing
# ===========================================
# All ERP instances under erp.vumacloud.com
#
# Routing:
#   erp.vumacloud.com        → VumaCloud internal ERP (db: vumacloud)
#   *.erp.vumacloud.com      → Customer instances (acme.erp.vumacloud.com → db=acme)

# Map subdomain to database name
# Extracts "acme" from "acme.erp.vumacloud.com"
map $host $db_name {
    ~^(?<subdomain>[^.]+)\.erp\.vumacloud\.com$ $subdomain;
    default "";
}

# ===========================================
# HTTP → HTTPS Redirect
# ===========================================
server {
    listen 80;
    listen [::]:80;
    server_name erp.vumacloud.com *.erp.vumacloud.com;

    # Block bots at HTTP level too
    location /robots.txt {
        return 200 "User-agent: *\nDisallow: /\n";
        add_header Content-Type text/plain;
    }

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# ===========================================
# erp.vumacloud.com - VumaCloud Internal ERP
# ===========================================
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name erp.vumacloud.com;

    # SSL certificate for vumacloud.com
    ssl_certificate /etc/letsencrypt/live/vumacloud.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/vumacloud.com/privkey.pem;
    ssl_session_timeout 1d;
    ssl_session_cache shared:SSL:50m;
    ssl_session_tickets off;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;

    # No indexing for ERP
    add_header X-Robots-Tag "noindex, nofollow, nosnippet, noarchive" always;

    # Security headers
    add_header Strict-Transport-Security "max-age=63072000" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Set database cookie to vumacloud (VumaCloud's internal ERP)
    add_header Set-Cookie "db=vumacloud; Path=/; Secure; HttpOnly; SameSite=Lax" always;

    # Robots.txt - block indexing
    location /robots.txt {
        return 200 "User-agent: *\nDisallow: /\n";
        add_header Content-Type text/plain;
    }

    # Proxy headers
    proxy_read_timeout 720s;
    proxy_connect_timeout 720s;
    proxy_send_timeout 720s;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Odoo-dbfilter vumacloud;

    # Client body size for file uploads
    client_max_body_size 100M;

    # Odoo longpolling
    location /longpolling {
        proxy_pass http://odoo:8072;
    }

    # Odoo websocket
    location /websocket {
        proxy_pass http://odoo:8072;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # Rate limit login attempts
    location ~ ^/(web/login|web/session/authenticate) {
        limit_req zone=login burst=10 nodelay;
        proxy_pass http://odoo:8069;
    }

    # Rate limit API
    location /api/ {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://odoo:8069;
    }

    # Static files caching
    location ~* /web/static/ {
        proxy_cache_valid 200 90m;
        proxy_buffering on;
        expires 7d;
        proxy_pass http://odoo:8069;
    }

    # Root redirect to login
    location = / {
        return 302 /web/login;
    }

    # Main Odoo
    location / {
        proxy_pass http://odoo:8069;
        proxy_redirect off;
    }

    # Gzip
    gzip on;
    gzip_types text/css text/scss text/plain text/xml application/xml application/json application/javascript;
}

# ===========================================
# *.erp.vumacloud.com - Customer Instances
# ===========================================
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name *.erp.vumacloud.com;

    # Wildcard SSL certificate for *.erp.vumacloud.com
    ssl_certificate /etc/letsencrypt/live/vumacloud.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/vumacloud.com/privkey.pem;
    ssl_session_timeout 1d;
    ssl_session_cache shared:SSL:50m;
    ssl_session_tickets off;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;

    # No indexing for customer ERPs
    add_header X-Robots-Tag "noindex, nofollow, nosnippet, noarchive" always;

    # Security headers
    add_header Strict-Transport-Security "max-age=63072000" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Set database cookie based on subdomain
    add_header Set-Cookie "db=$db_name; Path=/; Secure; HttpOnly; SameSite=Lax" always;

    # Robots.txt - block everything
    location /robots.txt {
        return 200 "User-agent: *\nDisallow: /\n";
        add_header Content-Type text/plain;
    }

    # Proxy headers
    proxy_read_timeout 720s;
    proxy_connect_timeout 720s;
    proxy_send_timeout 720s;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Odoo-dbfilter $db_name;

    # Client body size for file uploads
    client_max_body_size 100M;

    # Odoo longpolling
    location /longpolling {
        proxy_pass http://odoo:8072;
    }

    # Odoo websocket
    location /websocket {
        proxy_pass http://odoo:8072;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # Rate limit login attempts
    location ~ ^/(web/login|web/session/authenticate) {
        limit_req zone=login burst=10 nodelay;
        proxy_pass http://odoo:8069;
    }

    # Rate limit API
    location /api/ {
        limit_req zone=api burst=50 nodelay;
        proxy_pass http://odoo:8069;
    }

    # Static files caching
    location ~* /web/static/ {
        proxy_cache_valid 200 90m;
        proxy_buffering on;
        expires 7d;
        proxy_pass http://odoo:8069;
    }

    # Root redirect to login
    location = / {
        return 302 /web/login;
    }

    # Main Odoo
    location / {
        proxy_pass http://odoo:8069;
        proxy_redirect off;
    }

    # Gzip
    gzip on;
    gzip_types text/css text/scss text/plain text/xml application/xml application/json application/javascript;
}
